language: go

services:
  - docker

before_install:
  - docker build -t stolarskis/goplant .
  - docker run -d stolarskis/goplant /bin/sh -c "cd /root/goplant; bundle exec foreman start;"
  - docker ps -a
  - docker run stolarskis/goplant /bin/sh -c "cd /root/goplant"